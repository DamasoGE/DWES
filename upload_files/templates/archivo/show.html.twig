{% extends 'base.html.twig' %}

{% block title %}Archivo{% endblock %}

{% block body %}
    <h1>Archivo</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ archivo.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ archivo.name }}</td>
            </tr>
            <tr>
                <th>Extension</th>
                <td>{{ archivo.extension }}</td>
            </tr>
            <tr>
                <th>Filename</th>
                <td>{{ archivo.filename }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_archivo_index') }}">back to list</a>

    <a href="{{ path('app_archivo_edit', {'id': archivo.id}) }}">edit</a>

    {{ include('archivo/_delete_form.html.twig') }}
{% endblock %}
