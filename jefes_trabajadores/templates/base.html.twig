<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Bienvenido!{% endblock %}</title>
        <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text><text y=%221.3em%22 x=%220.2em%22 font-size=%2276%22 fill=%22%23fff%22>sf</text></svg>">
        {% block stylesheets %}
        {% endblock %}

        {% block javascripts %}
        {% endblock %}
    </head>
    <body>
        {% if app.user %}
            <div class="mb-3">
                Has iniciado sesión como {{ app.user.userIdentifier }}, <a href="{{ path('app_logout') }}">Cerrar Session</a>
            </div>

            <div><a href="{{ path("app_main") }}"> Página principal </a></div>
            <div><a href="{{ path("app_stockchange_index") }}"> Gestor de Inventario </a></div>
            
            {% if is_granted('ROLE_ADMIN') %}
            <div><a href="{{ path("app_item_index") }}">Gestor de artículos (ADMIN)</a></div>
            {% endif %}

        {% endif %}


        {% block body %}{% endblock %}
    </body>
</html>
